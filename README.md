# Student Management System

**Student Management System** — a beginner-friendly Python CLI app using SQLite to manage student records (Add / View / Search / Update / Delete). Built step-by-step to learn Python + SQL integration.

## Features
- Add student (name, roll number, course, marks)
- View all students
- Search student by roll
- Search students by name (partial match)
- Update course / marks
- Delete student with confirmation

## How to run (local)
1. Open terminal in project folder.
2. (Optional) create virtual environment:
   ```bash
   python -m venv venv
   # macOS / Linux
   source venv/bin/activate
   # Windows (PowerShell)
   venv\Scripts\Activate.ps1

